import RNMaps, { Marker as RNMarker, Circle as RNCircle, PROVIDER_GOOGLE as RN_PROVIDER_GOOGLE } from "react-native-maps";

export const MapView = RNMaps;
export const Marker = RNMarker;
export const Circle = RNCircle;
export const PROVIDER_GOOGLE = RN_PROVIDER_GOOGLE;
export const isMapSupported = true;
